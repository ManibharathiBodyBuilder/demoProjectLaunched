
spring.application.name=demoproject

# === PostgreSQL Render DB connection ===
spring.datasource.url=jdbc:postgresql://dpg-cq0ab12345-p.render.com:5432/prod_db
spring.datasource.username=demouser
spring.datasource.password=Mani@2025
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === Server Port ===
server.port=8080   # \u2705 Use 8080 (Render expects this by default)

# === Logging ===
logging.level.com.DemoProject=DEBUG

# === Hikari Connection Pool ===
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

